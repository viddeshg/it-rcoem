<!DOCTYPE html>
<html>
<head>
  <title>Edit | <%= @event.id %></title>
  <%= render 'layouts/links' %>
  <%= render 'layouts/styles' %>

  <style>
    i input
    {
      color: white !important;
    }
  </style>
</head>
<body>
<%= render 'layouts/navbar' %>
<main>
  <div class="container">
    <br><br>
    <div class="row">
      <div class="col s12 m10 offset-m1">
        <%= render 'events/formNew' %>
      </div>
      <div class="col s12 m10 offset-m1">
        <table class="s12 m12 striped">
          <thead style="color:#673BB7;">
          <tr>
            <th>S.No.</th>
            <th>Date</th>
            <th>Notice Title</th>
          </tr>
          </thead>
          <tbody>
          <% @events.each do |event| %>
              <tr>
                <td><%= @flag = @flag + 1 %></td>
                <td><%= event.date %></td>
                <td><a style="color:#333;" href="/notices/<%= event.id %>"><%= event.title %></a></td>
              </tr>
          <% end %>
          </tbody>
        </table>


      </div>
    </div>
  </div>
</main>
<%= render 'layouts/footer' %>
<%= render 'layouts/scripts' %>
<script>
    $(document).ready(function() {
        $('input#notice_name').characterCounter();
    });
</script>
</body>
</html>
